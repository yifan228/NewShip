using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CargoAttack : MonoBehaviour
{
    [SerializeField] LevelHandller levelHandller;
    [SerializeField] OnTriggerAttack triggerAttack;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }
}
